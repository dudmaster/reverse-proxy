name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: npm ci

      - name: Run test script
        run: echo Hello, world!

      - name: Run reverse proxy script
        run: |
          cd ♯.sh file dir
          chmod +x ./script.sh
          ./script.sh
